// src/app/(public)/PublicLayout.js

import PublicLayout from "@/components/Layout/PublicLayout/PublicLayout";

export default function PublicRouteLayout({ children }) {
  return <PublicLayout>{children}</PublicLayout>;
}
